defmodule ElixirScope.Test.Query.BuilderTest do
  use ExUnit.Case, async: true

  @moduletag :unit

  alias ElixirScope.Query.Builder
  alias ElixirScope.Test.Support.Helpers
  alias ElixirScope.Test.Fixtures

  setup do
    # Setup test data
    {:ok, %{}}
  end

  describe "Query.Builder" do
    test "placeholder test for Query.Builder" do
      # TODO: Implement unit test
      assert true
    end
  end
end
